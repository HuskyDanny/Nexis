apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: nexis
  labels:
    app: nexis
    component: backend
spec:
  selector:
    app: nexis
    component: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
