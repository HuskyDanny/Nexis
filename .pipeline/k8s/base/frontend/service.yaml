apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: nexis
  labels:
    app: nexis
    component: frontend
spec:
  selector:
    app: nexis
    component: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
