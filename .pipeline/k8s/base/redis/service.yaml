apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: nexis
  labels:
    app: nexis
    component: redis
spec:
  selector:
    app: nexis
    component: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
